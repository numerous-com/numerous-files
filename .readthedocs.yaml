# .readthedocs.yaml
version: 2

mkdocs:
  configuration: mkdocs.yml

build:
  os: "ubuntu-20.04"
  tools:
    - python

python:
  version: 3.12
  install:
    - method: pip
      path: .
      extra_requirements:
        - dev  # Install dev dependencies as well
